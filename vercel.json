{
  "buildCommand": "echo 'Backend only'",
  "outputDirectory": ".",
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.ts": {
      "runtime": "edge",
      "maxDuration": 60
    }
  }
}
